name: clean-architecture-kotlin
services:
  postgres:
    image: postgres:18-alpine
    restart: always
    ports:
      - "6686:5432"
    environment:
      POSTGRES_USER: ca_kotlin
      POSTGRES_PASSWORD: ca_kotlin
      POSTGRES_DB: ca_kotlin
